# DeliveryAutomation 코딩 규칙

## 1. 패턴을 지키자
- 기존 코드의 네이밍 컨벤션, 파일 구조, 아키텍처 패턴을 일관되게 유지한다.
- 새 기능 추가 시 기존 유사 코드의 구조를 따른다.
- Kotlin 코딩 컨벤션(camelCase 함수/변수, PascalCase 클래스)을 준수한다.

## 2. One Source of Truth
- 동일한 데이터/상태는 단 한 곳에서만 관리한다.
- 모든 설정값은 `AppPrefs`를 통해서만 접근한다.
- 앱 패키지명, URI 스킴 등 식별자는 중복 정의하지 않는다.
- 상태를 여러 곳에서 따로 저장하지 않는다.

## 3. 하드코딩 하지 말자
- 앱 패키지명, URI 스킴, 파일 경로, 문자열 상수는 반드시 `const val` 또는 리소스로 정의한다.
- 매직 넘버/문자열을 코드 내 직접 삽입하지 않는다.
- 설정 키(SharedPreferences key)는 상수로 선언한다.

## 4. 에러 처리(예외사항)를 잘하자
- 외부 API 호출, Intent 실행, 파일 I/O에는 반드시 try-catch를 사용한다.
- 예외 발생 시 로그를 남기고(`NotificationLogWriter` 활용), 앱이 크래시되지 않도록 한다.
- null 가능성이 있는 값은 안전 호출 연산자(`?.`, `?:`)를 사용한다.
- 사용자에게 의미 있는 피드백(Toast, Log)을 제공한다.

## 5. 함수는 한 가지 책임만 가진다 (Single Responsibility)
- 하나의 함수는 하나의 역할만 수행한다.
- 함수가 두 가지 이상의 일을 한다면 분리한다.
- 함수명은 동작을 명확히 설명해야 한다 (`pauseMusic()`, `redirectNavigation()` 등).
- 함수 길이가 30줄을 초과하면 분리를 검토한다.
